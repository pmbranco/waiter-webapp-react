import {enums} from '../../../helpers';

// ----------------------------------------------------------------------
// ----------------------------------------------------------------------
// ----------------------------------------------------------------------

const PAGE_LOADING = 'PAGE_LOADING';

// ----------------------------------------------------------------------
// ----------------------------------------------------------------------
// ----------------------------------------------------------------------

export default {
  get: PAGE_LOADING,
  getFailure: `${PAGE_LOADING}_${enums.failure}`,
  getRequest: `${PAGE_LOADING}_${enums.request}`,
  getSuccess: `${PAGE_LOADING}_${enums.success}`
}
